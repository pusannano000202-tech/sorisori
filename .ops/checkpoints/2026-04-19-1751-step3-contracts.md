# Checkpoint

- Date: 2026-04-19 17:51
- Branch: main
- Topic: Phase 0 / Step 3 - shared contracts
- Files changed:
  - `packages/contracts/package.json`
  - `packages/contracts/README.md`
  - `packages/contracts/tsconfig.json`
  - `packages/contracts/src/audio.ts`
  - `packages/contracts/src/providers.ts`
  - `packages/contracts/src/session.ts`
  - `packages/contracts/src/events.ts`
  - `packages/contracts/src/mvp.ts`
  - `packages/contracts/src/index.ts`
  - `apps/web/package.json`
  - `apps/web/tsconfig.json`
  - `apps/web/next.config.ts`
  - `apps/web/src/app/session/page.tsx`
  - `apps/web/src/app/history/page.tsx`
  - `.ops/task-log.md`
  - `package-lock.json`
- Decisions made:
  - 공통 세션 언어는 `@sorisori/contracts` 패키지로 모은다.
  - web은 계약 패키지에서 타입뿐 아니라 MVP 기본 상수도 import 할 수 있게 둔다.
  - Next web 앱은 `transpilePackages`로 workspace 패키지 import를 허용한다.
- Commands run:
  - `npm install`
  - `npm run check -w @sorisori/contracts`
  - `npm run lint -w @sorisori/web`
  - `npm run typecheck -w @sorisori/web`
  - `npm run build -w @sorisori/web`
- Validation result:
  - contracts 타입 검사 통과
  - web lint 통과
  - web typecheck 통과
  - web build 통과
- Remaining work:
  - contracts에 API 요청/응답 스키마는 아직 없음
  - realtime/pipeline 서비스는 아직 contracts를 사용하지 않음
  - desktop 앱 스캐폴드는 아직 없음
  - 실제 스트리밍/저장 로직은 아직 없음
- Next immediate step:
  - `apps/desktop`에 Tauri 골격을 만들고, WASAPI loopback 캡처 모듈 자리를 잡는다.
- Resume prompt:
  - `docs/TRD.md`와 `packages/contracts/src/*`를 먼저 읽고, apps/desktop에 Tauri 프로젝트 골격과 Windows 오디오 캡처 모듈 자리를 만들어. contracts의 오디오 포맷과 세션 상태 타입을 desktop 쪽 설계 기준으로 재사용해.`
