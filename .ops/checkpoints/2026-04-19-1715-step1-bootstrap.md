# Checkpoint

- Date: 2026-04-19 17:15
- Branch: main
- Topic: Phase 0 / Step 1 - repo bootstrap
- Files changed:
  - `docs/TRD.md`
  - `.gitignore`
  - `README.md`
  - `package.json`
  - `apps/*`
  - `services/*`
  - `packages/*`
  - `.ops/task-log.md`
- Decisions made:
  - 저장소는 `npm workspaces` 기준으로 시작한다.
  - Windows MVP 시스템 오디오 캡처는 `wasapi` 중심으로 간다.
  - 캡처 직후 `float32/stereo/44.1 or 48kHz -> PCM16/mono/24kHz` 변환 계층을 둔다.
- Commands run:
  - `git init -b main`
  - `npm run dev:web`
  - `npm run dev:desktop`
  - `npm run check:contracts`
- Validation result:
  - Git 저장소 초기화 성공
  - 루트 workspace 스크립트 연결 성공
  - `@sorisori/web`, `@sorisori/desktop`, `@sorisori/contracts` placeholder 스크립트 실행 성공
- Remaining work:
  - 웹 앱 실제 스캐폴드 없음
  - Tauri 실제 프로젝트 없음
  - 공통 타입/이벤트 정의 없음
  - 오디오 캡처 코드 없음
- Next immediate step:
  - `apps/web`에 실제 Next.js App Router 골격 생성
- Resume prompt:
  - `docs/PRD.md`, `docs/TRD.md`, .ops/task-log.md 를 먼저 읽고, apps/web에 Next.js 골격을 생성해. 현재 저장소는 npm workspaces 기반이며 desktop은 나중에 Tauri로 붙일 예정이니 web 작업과 충돌하지 않게 진행해.`
