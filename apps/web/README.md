# Web App

Next.js App Router 기반 웹 앱입니다.

현재 포함된 화면:

- `/` 랜딩 페이지
- `/session` 실시간 뷰어 + 세션 라우팅 화면
- `/history` 세션 기록 화면
- `/session/[id]` 세션 상세 아카이브 화면
- `/api/health` 기본 헬스 체크

다음 단계:

- 실세션 live 검증
- `/session?id=...` 와 desktop/pipeline 세션 ID 정렬 검증
- 기록 export / 복원 UX 정리
